engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  eslint:
    enabled: true
  fixme:
    enabled: true

# Exclude test directory
exclude_paths:
- test/**

ratings:
  paths:
    - "**.js"
