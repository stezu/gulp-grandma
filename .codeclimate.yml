engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  eslint:
    enabled: true
    channel: "eslint-2"
  fixme:
    enabled: true

# Exclude test directory
exclude_paths:
- test/**

ratings:
  paths:
    - lib/**
    - "**.js"
